#!/bin/bash

echo "Here is daemonize"

cleanup() {
  kill -s SIGTERM $!                                                     
  exit 0
}

trap cleanup SIGINT SIGTERM

while sleep 10; do
  wait $!
  echo "deamonize"
done